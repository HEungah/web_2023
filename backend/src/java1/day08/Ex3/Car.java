package java1.day08.Ex3;

// 사용목적 : 설계도
public class Car {	// class s
	// 1.필드 : 객체의 데이터를 저장하는곳(변수와 비슷하지만 다름)
	String model;	// 선언시 초기화 값이 없으므로 기본값인 null 저장
	boolean Start;	// 선언시 초기화 값이 없으므로 기본값인 false 저장
	int speed;	// 선언시 초기화 값이 없으므로 기본값인 0 저장
	// 2.생성자 : 객체 생성시 초기화 역할 담당(클래스명과 동일하게 선언)
	
	// 3.메소드 : 객체의 동작으로 호출시 샐행되는 블록
	

	
	
	
	
	
}	// class e





















