package java1.day09.Ex1;

public class Car {
	// 1.필드 : 객체의 데이터를 저장하는 곳
	
	// 2.생성자 : 객체 생성시 초기화 역할 담담
		// * 생성자명 : 클래스명과 동일
		// * 메소드와 비슷하지만 리턴값이 없음( 인수(매개값)은 존재)
		// * 생성자명(타입 매개변수, 타입 매개변수, 타입 매개변수)
		// * {실행코드}
		// 실행조건 : 1. 매개변수로 전달된 값을 필드에 저장 2. 필드에 저장하기 전에 유효성 검사
	public Car() {}
	Car(String model, String color, int maxSpeed){
		
	}
	// 3.메소드
	
}
